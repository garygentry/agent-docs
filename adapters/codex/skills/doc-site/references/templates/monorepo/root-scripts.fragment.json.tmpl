// Merge into the ROOT package.json — register the docs package + passthrough
// scripts (06 §7.3-§7.4). This is an additive key-merge, NOT a full-file copy:
// add `workspaces` membership (npm only) idempotently and set scripts.dev:docs /
// scripts.build:docs. If a script key already exists with a DIFFERENT value
// (user-edited), apply never-clobber: skip + flag RERUN_SKIP (00 §3.3/§7).
//
// TWO fragments are provided; the agent selects ONE by detected {{PKG_MANAGER}}
// (no in-template logic — 00 §4):
//
// --- npm fragment (ACTIVE): {{PKG_MANAGER}}=npm — carries `workspaces` ---
{
  "workspaces": ["{{DOCS_PKG_DIR}}"],
  "scripts": {
    "dev:docs": "npm run dev --workspace {{DOCS_PKG_DIR}}",
    "build:docs": "npm run build --workspace {{DOCS_PKG_DIR}}"
  }
}
//
// --- pnpm fragment: {{PKG_MANAGER}}=pnpm — NO `workspaces` (pnpm uses
//     pnpm-workspace.yaml); only the `scripts` keys merge into root package.json:
// {
//   "scripts": {
//     "dev:docs": "pnpm --filter ./{{DOCS_PKG_DIR}} dev",
//     "build:docs": "pnpm --filter ./{{DOCS_PKG_DIR}} build"
//   }
// }
